function path = ViterbiDecode(p_start, A, B)
[N,Q] = size(B);
path = zeros(N,1);
PATH = zeros(N,Q);

tmp = log(p_start)' + log(B(1,:));
tmp = normalise(tmp);
for i1 = 2:N
    tmp = tmp * A .* B(i1,:)
    
end

end